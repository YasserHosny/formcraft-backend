-- Seed data: Initial admin user
-- Note: Run this AFTER creating the admin user via Supabase Auth Dashboard or CLI.
-- Replace the UUID below with the actual auth.users ID of your admin account.

-- Example (uncomment and replace UUID after creating admin user):
-- UPDATE public.profiles SET role = 'admin' WHERE id = 'your-admin-user-uuid-here';
